# testdata

Fixture Codex home used by tests (`CODEX_HOME` layout):

```
testdata/codex-home/sessions/YYYY/MM/DD/rollout-*.jsonl
```

This mirrors `~/.codex/sessions`. Do not point `CODEX_HOME` at `testdata/` itself; use `testdata/codex-home`.
